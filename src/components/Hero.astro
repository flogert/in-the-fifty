<section id="home" class="hero" aria-labelledby="hero-title">
  <div class="hero-content">
    <div class="hero-grid">
      <div class="hero-copy">
        <div class="hero-logo-frame">
          <img class="hero-logo" src="/inthefifty.jpg" alt="In The Fifty logo" decoding="async" fetchpriority="high" />
        </div>
        <p class="hero-eyebrow">Studio50 Live</p>
        <h1 class="hero-title" id="hero-title">
          <span>Stay in touch for updates</span>
        </h1>
        <p class="hero-subtitle">
          Follow along for upcoming features, new American made brands, and the latest Studio50 news.
        </p>
        <div class="hero-details" aria-label="Update details">
          <span>Studio50 updates</span>
          <span>American made brands</span>
          <span>Feature announcements</span>
        </div>
        <div class="hero-actions">
          <a href="#studio50" class="btn btn-primary">Explore Studio50</a>
          <a href="#contact" class="btn btn-outline">Stay in Touch</a>
        </div>
      </div>

      <aside class="countdown-card" aria-labelledby="countdown-title">
        <p class="countdown-eyebrow" id="countdown-title">Stay Connected</p>
        <div class="countdown-grid" aria-label="Ways to stay updated">
          <div class="countdown-unit">
            <span class="countdown-value">01</span>
            <span class="countdown-label">Send a lead</span>
          </div>
          <div class="countdown-unit">
            <span class="countdown-value">02</span>
            <span class="countdown-label">Watch for news</span>
          </div>
          <div class="countdown-unit">
            <span class="countdown-value">03</span>
            <span class="countdown-label">Share the brand</span>
          </div>
          <div class="countdown-unit">
            <span class="countdown-value">04</span>
            <span class="countdown-label">Check back soon</span>
          </div>
        </div>
        <p class="countdown-note">Reach out through the contact section to get the latest Studio50 updates and feature announcements.</p>
      </aside>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    align-items: center;
    justify-content: center;
    background:
      radial-gradient(circle at 20% 18%, rgba(255, 255, 255, 0.34), transparent 24%),
      radial-gradient(circle at 78% 24%, rgba(178, 34, 52, 0.26), transparent 26%),
      linear-gradient(135deg, #b22234 0%, #f7f8fc 46%, #3c3b6e 100%);
    overflow: hidden;
    padding: calc(var(--header-height, 72px) - 0.65rem) 1rem clamp(2rem, 5vh, 3rem);
  }

  .hero::before,
  .hero::after {
    content: '';
    position: absolute;
    width: 300px;
    height: 300px;
    pointer-events: none;
    filter: blur(10px);
    animation: driftGlow 7s ease-in-out infinite;
  }

  .hero::before {
    top: -60px;
    right: -80px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.42), transparent 72%);
  }

  .hero::after {
    bottom: -110px;
    left: -85px;
    background: radial-gradient(circle, rgba(60, 59, 110, 0.34), transparent 70%);
    animation-delay: 0.8s;
  }

  .hero-content {
    position: relative;
    width: min(100%, 72rem);
    color: var(--color-dark);
    z-index: 2;
    animation: fadeScaleSoft 0.8s ease-out both;
  }

  .hero-grid {
    display: grid;
    gap: 2rem;
    align-items: center;
  }

  .hero-copy {
    text-align: center;
  }

  .hero-logo-frame {
    display: flex;
    justify-content: center;
    width: 100%;
    margin: 0 auto 0.85rem;
  }

  .hero-logo {
    display: block;
    width: min(100%, 8.5rem);
    border-radius: 1.2rem;
    box-shadow: 0 16px 36px rgba(18, 24, 48, 0.18);
  }

  .hero-eyebrow {
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--color-red);
    margin-bottom: 1.15rem;
  }

  .hero-title {
    font-size: clamp(2.2rem, 9vw, 4.25rem);
    font-weight: 900;
    font-family: var(--font-display);
    line-height: 1.1;
    margin-bottom: 1rem;
    letter-spacing: -0.01em;
    text-wrap: balance;
  }

  .hero-title span {
    display: inline-block;
    margin-top: 0.45rem;
    padding: 0.16em 0.3em;
    border-radius: 0.35rem;
    color: var(--color-white);
    background: linear-gradient(90deg, var(--color-red), var(--color-blue));
    box-shadow: 0 12px 26px rgba(18, 24, 48, 0.16);
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.2rem);
    color: #27304b;
    max-width: 34rem;
    margin: 0 auto 1.5rem;
    line-height: 1.65;
  }

  .hero-details {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.75rem;
    margin: 0 0 1.75rem;
  }

  .hero-details span {
    display: inline-flex;
    align-items: center;
    padding: 0.58rem 0.82rem;
    background: rgba(255, 255, 255, 0.8);
    border: 1px solid rgba(60, 59, 110, 0.16);
    border-radius: 0.4rem;
    box-shadow: 0 10px 30px rgba(18, 24, 48, 0.08);
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-blue);
    animation: fadeUpSoft 0.8s ease-out both;
  }

  .countdown-card {
    width: min(100%, 27rem);
    margin: 0 auto;
    padding: 0;
    border: 0;
    background: transparent;
    box-shadow: none;
    backdrop-filter: none;
    text-align: center;
    animation: fadeScaleSoft 0.8s ease-out both;
  }

  .countdown-eyebrow {
    margin: 0 0 0.9rem;
    font-size: 0.76rem;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-red);
  }

  .countdown-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.85rem;
  }

  .countdown-unit {
    display: grid;
    gap: 0.2rem;
    padding: 1rem 0.6rem;
    background: rgba(255, 255, 255, 0.82);
    border: 1px solid rgba(60, 59, 110, 0.16);
    border-radius: 0.45rem;
    box-shadow: 0 12px 28px rgba(18, 24, 48, 0.08);
  }

  .countdown-value {
    font-family: var(--font-display);
    font-size: clamp(1.3rem, 5vw, 1.8rem);
    font-weight: 900;
    line-height: 1;
    color: var(--color-dark);
  }

  .countdown-label {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #5a627a;
  }

  .countdown-note {
    margin: 0.9rem 0 0;
    font-size: 0.85rem;
    line-height: 1.6;
    color: #3a4563;
  }

  .hero-actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 0;
  }

  .btn {
    display: inline-block;
    padding: 0.85rem 2rem;
    border-radius: 0.4rem;
    font-weight: 800;
    font-family: var(--font-display);
    font-size: 1rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    transition: transform 0.18s ease, background 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease;
  }

  .btn:hover {
    transform: translateY(-3px) scale(1.01);
  }

  .btn-primary {
    background: var(--color-red);
    color: var(--color-white);
    border: 2px solid var(--color-red);
    box-shadow: 0 16px 34px rgba(178, 34, 52, 0.22);
  }

  .btn-primary:hover {
    background: #8b1a27;
    border-color: #8b1a27;
  }

  .btn-outline {
    background: rgba(255, 255, 255, 0.34);
    color: var(--color-dark);
    border: 2px solid rgba(60, 59, 110, 0.22);
  }

  .btn-outline:hover {
    border-color: var(--color-blue);
    background: rgba(255, 255, 255, 0.64);
  }

  @media (min-width: 900px) {
    .hero {
      min-height: 100vh;
      padding: calc(var(--header-height, 72px) - 0.4rem) 1.25rem 3.25rem;
    }

    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .hero-copy {
      width: min(100%, 46rem);
      margin: 0 auto;
    }

    .countdown-card {
      width: min(100%, 30rem);
      margin: 0 auto;
    }
  }

  @media (max-width: 899px) {
    .hero {
      min-height: auto;
      padding: calc(var(--header-height, 72px) - 0.65rem) 1rem 2rem;
    }

    .hero-title span {
      margin-left: auto;
      margin-right: auto;
    }

    .hero-subtitle {
      margin-left: auto;
      margin-right: auto;
    }

    .hero-details,
    .hero-actions {
      justify-content: center;
    }

    .countdown-card {
      margin: 0 auto;
      width: min(100%, 30rem);
    }
  }

  @media (max-width: 520px) {
    .hero-title {
      font-size: clamp(2rem, 12vw, 3rem);
    }

    .countdown-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
</style>
